from sklearn.naive_bayes import GaussianNB
import numpy as np

def run(test, train, val, func, layers, nodes, solver):
    print("Running Best Multi Layer Perceptron...")

    print("Done!")

    return 0
